runs_base: runs
tranco_cache: tranco_top1m.csv

classifier:
  default: rule
  model_dir: /path/to/external/classifier_checkpoint
  device: cpu
  batch_size: 8

crawls:
  - name: DEMO
    run_root: runs/demo
