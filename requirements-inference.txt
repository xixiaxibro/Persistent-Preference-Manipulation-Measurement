# Additional runtime dependencies for the active semantic Stage 02 classifier.
# Install alongside the base requirements.txt:
#   python3 -m pip install -r requirements.txt -r requirements-inference.txt

torch>=2.0.0,<3.0.0
transformers>=4.36.0,<5.0.0
numpy>=1.24.0,<3.0.0